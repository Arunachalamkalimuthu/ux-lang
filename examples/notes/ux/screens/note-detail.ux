screen NoteDetail(note)
  intent "Read and edit one note"

  use NoteRow(note)
  show note.body

  action togglePin(note)
  action "Back" -> AllNotes

component NoteRow(note)
  show note.title
  show note.updated
